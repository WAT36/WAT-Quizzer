{
  "name": "wat-quizzer",
  "version": "1.0.0",
  "description": "csv_quiz_bot 2nd edition by WAT",
  "private": true,
  "workspaces": [
    "backend-nest",
    "frontend-next",
    "quizzer-lib",
    "batch"
  ],
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "build:backend": "npx -w backend-nest prisma format && npx -w backend-nest prisma generate && npx -w backend-nest nest build",
    "build:frontend": "npx -w frontend-next next build",
    "deps:check": "npx syncpack list-mismatches",
    "deps:fix": "npx syncpack fix-mismatches",
    "start:backend": "npm run build -w quizzer-lib && npm install -w backend-nest && npm run build -w backend-nest && npm run start:dev -w backend-nest",
    "start:frontend": "npm install -w frontend-next && npm run dev -w frontend-next"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/WAT36/WAT-Quizzer.git"
  },
  "author": "",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/WAT36/WAT-Quizzer/issues"
  },
  "homepage": "https://github.com/WAT36/WAT-Quizzer#readme",
  "devDependencies": {
    "@types/eslint-scope": "^3.7.4",
    "@typescript-eslint/eslint-plugin": "^8.32.1",
    "@typescript-eslint/parser": "^8.32.1",
    "eslint": "^9.27.0"
  }
}