export interface MessageState {
  message: string;
  messageColor: string;
}

export interface DisplayQuizState {
  quizSentense: string;
  quizAnswer: string;
  checked: boolean;
  expanded: boolean;
}
